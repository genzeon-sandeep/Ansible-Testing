name: Deploy to Local Server

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up SSH Agent
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.SSH_PRIVATE_KEY }}" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
          eval "$(ssh-agent -s)"
          ssh-add ~/.ssh/id_rsa
          ssh-keyscan github.com >> ~/.ssh/known_hosts

      - name: Debug SSH Private Key
        run: echo "${{ secrets.SSH_PRIVATE_KEY }}" | head -n 10

      - name: Test SSH Connection
        run: ssh -T git@github.com

      - name: Install Ansible
        run: sudo apt update && sudo apt install -y ansible

      - name: Debug - Check Current Directory
        run: pwd

      - name: Debug - List All Files
        run: ls -R

      - name: Run Ansible Playbook
        env:
          ANSIBLE_USER: ${{ secrets.ANSIBLE_USER }}
          ANSIBLE_HOST_KEY_CHECKING: False
        run: |
          cd ansible  # Change into the ansible directory
          ansible-playbook -i inventory.ini deploy.yml --private-key private_key.pem -u ${{ secrets.ANSIBLE_USER }}
